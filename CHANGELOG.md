## 0.1.0

- Initial release aligned with iOS FeedJarKit.
- `FeedJar.configure`, `FeedJar.submit`, and `FeedJar.defaultApiBaseUrl`.
- `FeedbackType` with `featureRequest` alias and display names.
- `FeedJarFeedbackModal` built-in UI (parity with `presentFeedback` / SwiftUI sheet).
- `FeedJarException` / `FeedJarErrorCode` matching iOS error cases.
