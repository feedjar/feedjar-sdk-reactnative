## 0.1.0

- Initial release of `@feedjar/sdk-react-native`.
- `configure`, `submit`, and `listFeedback` against `/ingest/feedback`.
- `FeedbackType` and support-email validation (parity with iOS / Web SDKs).
- Defaults to `https://api.feedjar.in` with `Authorization` and `X-FeedJar-Key` headers.
